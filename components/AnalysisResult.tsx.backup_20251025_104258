'use client';

import { AnalysisResponse } from '@/lib/types';
import { Download, CheckCircle } from 'lucide-react';

interface AnalysisResultProps {
  result: AnalysisResponse;
  onNewAnalysis: () => void;
}

export default function AnalysisResult({ result, onNewAnalysis }: AnalysisResultProps) {
  return (
    <div className="space-y-6 animate-fade-in">
      {/* Header */}
      <div className="bg-gradient-to-r from-purple-600 to-pink-600 text-white rounded-lg p-6">
        <div className="flex items-center justify-between">
          <div>
            <h2 className="text-2xl font-bold mb-2">🍰 Análise da Tiramisu</h2>
            <p className="text-purple-100">Fusão de Kotler × Gary Vee × Martha Gabriel</p>
          </div>
          <CheckCircle className="w-12 h-12" />
        </div>
      </div>

      {/* Summary */}
      <div className="bg-white rounded-lg shadow-lg p-6">
        <h3 className="text-xl font-bold mb-3 text-gray-800">📊 Resumo Executivo</h3>
        <p className="text-gray-700 leading-relaxed whitespace-pre-line">{result.summary}</p>
      </div>

      {/* 3 Trees Analysis */}
      <div className="bg-white rounded-lg shadow-lg p-6">
        <h3 className="text-xl font-bold mb-4 text-gray-800">🌳 Análise das 3 Árvores</h3>
        
        {/* Roots */}
        <div className="mb-4">
          <h4 className="font-semibold text-green-700 mb-2 flex items-center">
            🌱 RAÍZES - Diagnóstico Profundo
          </h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line pl-4">
            {result.three_trees.roots}
          </p>
        </div>

        {/* Trunk */}
        <div className="mb-4">
          <h4 className="font-semibold text-amber-700 mb-2 flex items-center">
            🌲 TRONCO - Execução Atual
          </h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line pl-4">
            {result.three_trees.trunk}
          </p>
        </div>

        {/* Branches */}
        <div>
          <h4 className="font-semibold text-emerald-700 mb-2 flex items-center">
            🍃 GALHOS - Melhorias e Alternativas
          </h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line pl-4">
            {result.three_trees.branches}
          </p>
        </div>
      </div>

      {/* Triad Insights */}
      <div className="bg-white rounded-lg shadow-lg p-6">
        <h3 className="text-xl font-bold mb-4 text-gray-800">👥 Visão da Tríade</h3>
        
        {/* Kotler */}
        <div className="mb-4 border-l-4 border-blue-500 pl-4">
          <h4 className="font-semibold text-blue-700 mb-2">🎓 KOTLER - Estratégia</h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line">
            {result.triad_insights.kotler}
          </p>
        </div>

        {/* Gary Vee */}
        <div className="mb-4 border-l-4 border-orange-500 pl-4">
          <h4 className="font-semibold text-orange-700 mb-2">🔥 GARY VEE - Execução</h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line">
            {result.triad_insights.gary_vee}
          </p>
        </div>

        {/* Martha */}
        <div className="border-l-4 border-purple-500 pl-4">
          <h4 className="font-semibold text-purple-700 mb-2">🤖 MARTHA - Tecnologia</h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line">
            {result.triad_insights.martha}
          </p>
        </div>
      </div>

      {/* Proposal */}
      <div className="bg-gradient-to-br from-purple-50 to-pink-50 rounded-lg shadow-lg p-6 border-2 border-purple-200">
        <h3 className="text-xl font-bold mb-4 text-gray-800">🎯 Proposta de Solução</h3>
        
        {/* Improved Version */}
        {result.proposal.improved_version && (
          <div className="mb-4 bg-white rounded-lg p-4">
            <h4 className="font-semibold text-purple-700 mb-2">✨ Versão Melhorada</h4>
            <div className="bg-gray-50 p-4 rounded border-l-4 border-purple-500">
              <p className="text-gray-700 leading-relaxed whitespace-pre-line font-mono text-sm">
                {result.proposal.improved_version}
              </p>
            </div>
          </div>
        )}

        {/* Action Plan */}
        <div className="mb-4 bg-white rounded-lg p-4">
          <h4 className="font-semibold text-purple-700 mb-2">📋 Plano de Ação</h4>
          <ul className="space-y-2">
            {result.proposal.action_plan.map((action, idx) => (
              <li key={idx} className="flex items-start">
                <span className="text-purple-600 mr-2">▶</span>
                <span className="text-gray-700">{action}</span>
              </li>
            ))}
          </ul>
        </div>

        {/* Key Recommendations */}
        <div className="mb-4 bg-white rounded-lg p-4">
          <h4 className="font-semibold text-purple-700 mb-2">💡 Recomendações-Chave</h4>
          <ul className="space-y-2">
            {result.proposal.key_recommendations.map((rec, idx) => (
              <li key={idx} className="flex items-start">
                <span className="text-pink-600 mr-2">✓</span>
                <span className="text-gray-700">{rec}</span>
              </li>
            ))}
          </ul>
        </div>

        {/* Expected Results */}
        <div className="bg-white rounded-lg p-4">
          <h4 className="font-semibold text-purple-700 mb-2">🎯 Resultados Esperados</h4>
          <p className="text-gray-700 leading-relaxed whitespace-pre-line">
            {result.proposal.expected_results}
          </p>
        </div>
      </div>

      {/* Sources */}
      <div className="bg-white rounded-lg shadow-lg p-6">
        <h3 className="text-xl font-bold mb-3 text-gray-800">📚 Fontes Consultadas (RAG)</h3>
        <div className="flex flex-wrap gap-2">
          {result.rag_sources.map((source, idx) => (
            <span
              key={idx}
              className="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-medium"
            >
              {source}
            </span>
          ))}
        </div>
      </div>

      {/* Actions */}
      <div className="flex gap-4">
        <button
          onClick={onNewAnalysis}
          className="flex-1 py-3 px-6 bg-gradient-to-r from-purple-600 to-pink-600 text-white rounded-lg font-semibold hover:from-purple-700 hover:to-pink-700 transition-all transform hover:scale-105"
        >
          🍰 Nova Análise
        </button>
        <button
          onClick={() => window.print()}
          className="py-3 px-6 bg-gray-200 text-gray-700 rounded-lg font-semibold hover:bg-gray-300 transition-all flex items-center gap-2"
        >
          <Download size={20} />
          Exportar
        </button>
      </div>
    </div>
  );
}
